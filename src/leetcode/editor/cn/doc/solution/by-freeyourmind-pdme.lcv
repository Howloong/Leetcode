* []

```python
MOD = 1000000007
d = {'7': 4, '9': 4, '2': 3, '3': 3, '4': 3, '5': 3, '6': 3, '8': 3}
class Solution:
    def countTexts(self, pressedKeys: str) -> int:
        n = len(pressedKeys)
        dp = [1] + [0] * n
        for i, ch in enumerate(pressedKeys):
            for j in range(i, i - d[ch], -1):
                if j < 0 or pressedKeys[j] != ch: break
                dp[i + 1] += dp[j]
            dp[i + 1] %= MOD
        return dp[n]
```

